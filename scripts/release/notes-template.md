## CLI Analyzer v<版本> — <一句话主题>

### 新功能 / 修复 / 改进
- …

### 下载
- macOS（Apple Silicon）：CLI-Analyzer-<v>-macos-arm64.dmg
- macOS（Intel）：CLI-Analyzer-<v>-macos-amd64.dmg
- Windows：CLI-Analyzer-<v>-windows-amd64-installer.exe / -portable.zip
- Linux：CLI-Analyzer-<v>-linux-amd64.deb / -tar.gz

> 未签名提示：macOS 首次打开请右键 → 打开；Windows 可能显示"未知发布者"。

---

## CLI Analyzer v<version> — <one-line theme>

### What's new / Fixed / Improved
- …

### Download
- macOS (Apple Silicon): CLI-Analyzer-<v>-macos-arm64.dmg
- macOS (Intel): CLI-Analyzer-<v>-macos-amd64.dmg
- Windows: CLI-Analyzer-<v>-windows-amd64-installer.exe / -portable.zip
- Linux: CLI-Analyzer-<v>-linux-amd64.deb / -tar.gz

> Unsigned builds: macOS right-click → Open on first launch; Windows may show "Unknown Publisher".
