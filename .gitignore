# 构建产物
build/bin
build/.metadata_never_index
# NSIS 构建时下载的 WebView2 引导程序等临时文件
build/windows/installer/tmp
bin
dist
frontend/dist

# 本地验证用 Go 工具链（临时解压，勿提交）
.go-toolchain

# 本地原型
docs/mockups

# 前端依赖
node_modules

# wails 生成代码（frontend/wailsjs + package.json.md5）随仓库提交：
# main.ts 直接 import 它们，CI/新 clone 在无 wails CLI 时也能跑 tsc 类型检查。
# wails 开发配置（build 时生成）
build/darwin/Info.dev.plist

# macOS
.DS_Store

# 本地工具 / 环境
.claude
.omo
.codex
.opencode
.codegraph
.pi

# 本地测试包（zip 与便携目录，勿提交）
build/package-test
build/*.zip
