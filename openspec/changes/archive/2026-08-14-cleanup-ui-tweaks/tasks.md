## 1. 0 字节子项可选

- [x] 1.1 `subRows` selectable 判定去掉 `bytes > 0` 门槛（有 id 即可勾选）
- [x] 1.2 0 字节行不再走无勾选框分支（统一渲染勾选框 + `hb(0)` = "0 B"）

## 2. logs 子项去「日志」标签

- [x] 2.1 子项类型标签条件增加 `s.kind !== 'logs'`（`_logs`/`*.log` 不再贴琥珀色标签）

## 3. 回归与交付

- [x] 3.1 前端测试 18/18 通过；tsc + vite build 通过
- [x] 3.2 重建 NSIS 安装器 + 便携 zip，更新 SHA256
